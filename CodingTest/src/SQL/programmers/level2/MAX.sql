-- 가격이 제일 비싼 상품을 출력

-- 정렬 후 LIMIT 사용
SELECT *
FROM FOOD_PRODUCT
ORDER BY PRICE DESC
LIMIT 1;

-- MAX 가격을 뽑아서 IN
SELECT *
FROM FOOD_PRODUCT
WHERE PRICE IN (
    SELECT MAX(PRICE)
    FROM FOOD_PRODUCT
    )